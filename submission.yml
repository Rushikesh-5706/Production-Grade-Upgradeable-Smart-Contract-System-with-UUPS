setup:
  commands:
    - npm install

test:
  commands:
    - npx hardhat test

docker:
  build:
    command: docker build -t token-vault-uups .
  run:
    command: docker run --rm token-vault-uups

verification:
  commands:
    - npx hardhat compile
    - npx hardhat test
